# Model Weights

Place the three pre-trained `.pt` files here after downloading from Google Drive:

- `audio-visual.pt`    (~226 MB)  — main AV model
- `language_model.pt`  (~128 MB)  — character-level LM
- `visual_frontend.pt` (~43 MB)   — CNN visual frontend

See the project README for the download link.
