# Core PyTorch (CPU version by default for deployment, change to cu118 etc if needed)
torch>=1.12.0
torchvision

# Inference Server
flask>=3.0.0

# Imaging and Audio
opencv-python>=4.2.0
scipy>=1.3.1
numpy

# Optional but helpful
editdistance
tqdm
matplotlib
